function item(name,type,damage) {
    //Instance Varaibles
    this.name = name;
    this.type = type;
    this.damage = null;

    if (this.type = 'Weapon') {
        this.damage = damage
    }

    //Methods

}

module.exports = item